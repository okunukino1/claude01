name: Add spot completion push test once

on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/add-spot-completion-push-test-once.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Debug workflow parser
        run: echo ok
