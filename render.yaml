services:
  - type: web
    name: company-chat
    runtime: node
    plan: free
    buildCommand: cd chat-app && npm install --include=dev && npx prisma generate && npx prisma migrate deploy && npm run build
    startCommand: cd chat-app && npm run start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: company-chat-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
      - key: ANTHROPIC_API_KEY
        sync: false

databases:
  - name: company-chat-db
    plan: free
