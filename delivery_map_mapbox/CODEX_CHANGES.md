# Claude / Codex 現状報告 — プロジェクト全体の状態 (2026-08-23)

## 0. Codex追記（2026-08-23 / test.122）

- 作業開始基準は最新 `origin/main` のコミット `3634f588db8cead1f30ad9c547c59cf28363b19a`。安定版は引き続き **v2026.07.10-1** で、通常版HTML・通常版APIは変更していない。
- テスト版を **v2026.06.24-test.122** へ更新した。配送先編集で完了にして地図へ戻る際、2秒以内に更新された現在地はすぐ使い、古い場合は画面を開いたままキャッシュ不可の高精度GPSを最長6秒だけ取得してから次の案内線を再計算する。
- 最新GPSの待機中は完了済み配送先への古い案内線とキャッシュを消す。最新位置を取得できなかった場合も、最後に確認できた位置から次の案内線を復旧し、地図操作を止めない。
- GPSの一時的な取得不能またはタイムアウトでは追跡状態を解除せず、従来の8秒待機ではなく1秒後に監視を再接続する。位置情報の許可を拒否された場合だけ追跡を解除し、自動再接続を止める。
- `scripts/test-delivery-gps-map-return.mjs` を追加し、新鮮なGPSの即時利用、古いGPSの再取得、取得失敗時の案内復旧、一時エラー時の追跡維持・1秒再接続、許可拒否時の停止、インラインJavaScript構文を確認する。

## 0. Codex追記（2026-08-20 / test.121）

- 作業開始基準は最新 `origin/main` のコミット `caa03c80a82981609ead0c9947ce7f3b862bf6a3`。安定版は引き続き **v2026.07.10-1** で、通常版HTML・通常版OCR APIは変更していない。
- テスト版を **v2026.06.24-test.121** へ更新した。2行・3行の住所を1行へ再構成する際、Cloud Vision候補が `3-26-12` まで読めているのに採用結果が `3-26` や `3-12` のように一部の数字を飛ばした場合を検出する。
- 数字の欠落を検出した写真だけ、従来のGemini画像OCRへ自動で戻して再確認する。全写真のAPI回数は増やさず、通常の写真はVisionとGeminiの併用結果をそのまま採用して速度を維持する。
- Vision文字選別と画像OCRの両方へ、番地の数字列を要約・省略せず、`address_lines` と完成住所の数字を出力前に照合する指示を追加した。数字の読み違いは今回の自動補正対象にせず、数字を飛ばしたと判断できる場合だけ再確認する。
- `scripts/test-delivery-hybrid-ocr.php` に末尾数字欠落、中間数字欠落、別番地、別町名、同じ町内の送り主除外を追加し、送り主候補を理由に不要な再読取を起こさないことも確認する。

## 0. Codex追記（2026-08-19 / test.120）

- 作業開始基準は最新 `origin/main` のコミット `9a47f9d`。安定版は引き続き **v2026.07.10-1** で、通常版HTML・通常版OCR APIは変更していない。
- テスト版を **v2026.06.24-test.120** へ更新し、伝票OCRの採用経路をCloud VisionとGeminiの併用へ切り替えた。テスト専用 `api/extract_address_hybrid_test.php` がVisionで全文を読み、その文字と住所候補をGeminiへ渡して、お届け先・建物名・部屋番号・宛名を整理する。
- Visionで文字を読めない、Geminiの選別が低信頼、番地不足、郵便番号矛盾、応答解析失敗の場合は、その写真だけ従来の `api/extract_address_test.php` によるGemini画像OCRへ戻す。回転再試行ではVisionを重複呼び出しせず、2回目以降は画像OCRだけを使う。
- Visionへの画像送信は、実機比較で非JSON HTTP 403を避けられたBase64 JSONを最初から使う。Visionの生OCR全文はGeminiへ渡すサーバー内処理だけに使用し、ブラウザー、比較記録、配送データへ返さない。
- `OCR読み比べ（試験）` に併用採用件数と画像OCRへ戻した件数を追加した。利用回数表示は運用中のVision呼び出しも数え、概算合計へVision分を含める。
- `scripts/test-delivery-hybrid-ocr.php` と拡張した `scripts/test-delivery-test-ocr-transport.mjs` で、文字選別の妥当性判定、個人情報全文を応答へ残さないこと、併用成功、画像OCRフォールバック、WAF 403再送、Vision重複防止を確認する。

## 0. Codex追記（2026-08-17 / test.119）

- 作業開始基準は最新 `origin/main` のコミット `c89499a4f13bba74b0bc32a2edc2f8e77d49f7c3`。安定版は引き続き **v2026.07.10-1** で、通常版HTML・通常版OCR APIは変更していない。
- テスト版を **v2026.06.24-test.119** へ更新した。実機の保存写真20枚で新Gemini OCRを確認したところ、15枚は正常応答した一方、5枚はGeminiへ届く前のホスティング保護機能と考えられる非JSONのHTTP 403になった。
- 通常は従来どおり軽量なJPEGバイナリを送信し、非JSONのHTTP 403だけをBase64入りJSONへ切り替えて1回再送する。アプリまたはGeminiがJSONで返す正式な403は再送せず、同じGemini呼び出しの重複を避ける。
- HTTPエラーを画像の向きの問題として回転再送しないようにした。OCRが正常応答したものの住所を読めない場合だけ、従来の向き自動リカバリーを続ける。
- `scripts/test-delivery-test-ocr-transport.mjs` を追加し、通常のバイナリ成功、非JSON 403からJSON再送成功、JSON 403で再送しないこと、HTTP 403で回転再送しないことを確認する。

## 0. Codex追記（2026-08-17 / test.118）

- 作業開始基準は最新 `origin/main` のコミット `deef6594367bae696c7c4dcfa5101b6462de5864`。安定版は引き続き **v2026.07.10-1** で、通常版HTML・通常版OCR APIは変更していない。
- テスト版を **v2026.06.24-test.118** へ更新した。Geminiの1回の構造化応答へ、お届け先の郵便番号・住所行を論理的な順序で返す `address_lines` を追加し、2行・3行・複数列の住所を1行へ再構成してからピン登録へ渡す。
- 番地が `43-` / `5`、`43` / `-5`、または丁目・番・号の途中で改行されても、行境界をまたいで結合する。建物名、部屋番号、宛名は既知の別項目を使って再構成候補から除外し、完成済み番地の後にある102・1001などの数字行を住所へ誤連結しない。
- Geminiが返した完成住所と住所行の郵便番号が矛盾する場合は自動上書きせず、完成住所を維持して自信度だけを下げる。GeminiのAPI呼び出し回数は増やさず、Cloud Visionは従来どおり比較専用で自動採用しない。
- OCR読み比べには生の住所行を追加保存せず、Geminiの住所行を採用して再構成した件数と、完成住所との矛盾件数だけを表示する。配送リストへ新しい警告は追加しない。
- `scripts/test-delivery-test-ocr.php` を追加し、改行位置の異なる番地、丁目・番・号、建物・部屋・宛名の除外、郵便番号矛盾、旧応答との互換性を固定データで確認する。

## 0. Codex追記（2026-08-17 / test.117）

- 作業開始基準は最新 `origin/main` のコミット `e16cd54fee656f59d2e793c0f34f87fa918ca7e6`。安定版は引き続き **v2026.07.10-1** で、通常版HTML・通常版APIは変更していない。
- テスト版を **v2026.06.24-test.117** へ更新した。`OCR読み比べ（試験）` の上から保存写真の比較確認を開くと、同じ重なり順位のため確認画面がOCR画面の背後へ隠れていた問題を修正した。
- 共通確認画面を通常モーダルより前へ表示するため、保存写真の対象枚数と `OK / キャンセル` が見える。写真送信やAPI呼び出しは従来どおり `OK` を押すまで開始しない。

## 0. Codex追記（2026-08-17 / test.116）

- 作業開始基準は最新 `origin/main` のコミット `45930f123b6288f3c3de89be4918a88602f74f2a`。安定版は引き続き **v2026.07.10-1** で、通常版HTML・通常版OCR APIは変更していない。
- テスト版を **v2026.06.24-test.116** へ更新し、`OCR読み比べ（試験）` に `保存写真で比較（最大20枚）` を追加した。端末のIndexedDBに残る直近20枚から、未比較またはVision失敗中の写真を新しい順に1枚ずつCloud Visionへ送る。
- 写真IDが一致する撮影時のGemini比較記録を再利用し、記録がない古い写真ではGeminiを追加呼び出ししない。Vision成功・文字なしまで完了済みの写真は再送せず、API利用前に対象枚数と最大呼び出し回数を確認する。
- 実行中は進捗を表示し、1枚の失敗で残りを止めない。配送先と伝票写真は削除・変更せず、生OCR全文も保存しない。撮影時のGemini記録がない写真はGemini一致率の母数から除外する。

## 0. Codex追記（2026-08-17 / Cloud Vision設定）

- Gemini専用キーとCloud Vision APIを組み合わせられないGoogle側の制限に対応し、GitHub Actionsの `GOOGLE_CLOUD_VISION_TEST_API_KEY` からテスト専用のサーバー秘密ファイルを生成する経路を追加した。キーはリポジトリ、ブラウザー、Actionsログへ出さず、`api/extract_address_vision_test.php` だけが優先して読む。
- 通常版HTML・通常版OCR API・既存Geminiキーは変更していない。Vision比較が設定されていない場合の非同期・非阻害動作も維持する。

## 0. Codex追記（2026-08-16 / test.115）

- 作業開始基準は最新 `origin/main` のコミット `a13c4bd29df535e57e44caeb4dfaeb3a5a636438`。安定版は引き続き **v2026.07.10-1** で、通常版HTML・通常版OCR APIは変更していない。
- テスト版を **v2026.06.24-test.115** へ更新し、現行Gemini OCRとGoogle Cloud Vision `DOCUMENT_TEXT_DETECTION` を同じ伝票写真で読み比べる影処理を追加した。ピン登録は従来どおりGemini結果を使い、Visionを待たず、Visionの設定不足・失敗・タイムアウトでも配送処理を止めない。
- Visionは1写真につき最初の向きで1回だけ呼び、Geminiの回転再試行回数を増やさない。テスト専用 `api/extract_address_vision_test.php` だけがAPIキーを保持し、専用キーが未設定ならサーバー内のGeminiキーを同一Google Cloudプロジェクトのキーとして試す。キーはブラウザーへ返さない。
- メニューへ `OCR読み比べ（試験）` を追加した。両OCRの成功数・平均時間と、利用者が編集画面で保存した内容に対する郵便番号・町名・住所全体・部屋番号の一致を確認できる。Visionは複数の住所候補に確認値が含まれるかを測る段階で、自動採用はしない。
- 比較記録は個別の生OCR全文を保存せず、候補と集計に必要な項目だけをこの端末のlocalStorageへ最大100件・7日間保存する。共有APIやほかの端末へ送らず、比較画面から配送先・伝票写真を残したまま削除できる。

## 0. Codex追記（2026-08-16 / test.114）

- 作業開始基準は `test.113` のコミット `d3c39db2bd6c2ba89b7cd7600b0cfec1551a2e1e`。安定版は引き続き **v2026.07.10-1** で、通常版HTML・通常版OCR APIは変更していない。
- 公開後、個人情報を含まないアプリアイコンを使った1回のOCR確認で、Geminiの旧 `responseSchema` が整数項目の数値 `enum` を受理せずHTTP 400になることを検出した。
- テスト版を **v2026.06.24-test.114** へ更新し、`rotation_hint` は整数型だけをスキーマで指定する。許可値の0・90・180・270への制限は従来どおりテスト専用APIの応答正規化で行う。

## 0. Codex追記（2026-08-16 / test.113）

- 作業開始基準は最新 `origin/main` のコミット `f38b3fdea360cd85c24bda1ef6e7fdbdf3ff02e1`。
- 安定版は引き続き **v2026.07.10-1**。通常版HTML・通常版OCR APIには今回の変更を反映していない。
- テスト版は **v2026.06.24-test.113**。Gemini OCRへテスト専用のレスポンススキーマを追加し、住所・郵便番号・建物名・部屋番号などを常に同じJSON形式で返すようにした。通常時のGemini呼び出し回数は増やしていない。
- JSONコードブロックや前後の余計な文字が混じった応答からも、文字列内の波括弧を壊さず最初の完全なJSONオブジェクトを取り出す。出力上限で応答が切れた場合は `finishReason` を見て区別する。
- 処理待ち写真の自動試行は最初の処理を含めて2回までとした。上限後は写真を削除せず `再処理が必要` へ移し、起動・復帰のたびに同じ赤い通知を繰り返さない。更新前から何度も失敗している保存写真も同じ扱いへ移行する。
- `保存・復旧状況` の `写真処理を再開` では手動再試行でき、撮り直し済みなら確認後に失敗写真だけを削除できる。

## 0. Codex追記（2026-08-12 / test.112）

- 作業開始基準は最新 `origin/main` のコミット `ce547e0f48567797f61c1b7d9f11892e86ad9a56`。
- 安定版は引き続き **v2026.07.10-1**。通常版HTML・通常版APIには今回の変更を反映していない。
- テスト版は **v2026.06.24-test.112**。配送モードの画面上部へ常時表示する `規制` ボタンを追加した。
- `規制` を押すと地図下部へ、`道路全体（1点）`、`一方通行・進入禁止（2点）`、`右左折・Uターン（3点）` の3種類だけを並べたクイック登録パネルを表示する。種類を選ぶと従来の地図タップ登録へ直接移る。
- 保存前の確認、Mapbox案内線への反映、同じ共有コードの端末との同期、登録回数の記録は既存処理をそのまま利用する。登録済み規制の確認・解除は従来どおりメニュー内に残した。
- クイック登録パネルは配送モードだけに表示し、入力モードと集荷画面では非表示にする。端末の戻るボタンではアプリを終了せず先にパネルを閉じる。

---

## 0. Codex追記（2026-08-12 / test.111）

- 作業開始基準は最新 `origin/main` のコミット `4f9935308e3ac8ae332d4336993699333515d57d`。
- 安定版は引き続き **v2026.07.10-1**。通常版HTML・通常版OCR API・通常版DB APIには今回の変更を反映していない。通常版DB APIの `manual=1` 保護と通常版HTML内の古い固定バージョン表示も、昇格指示がないため未変更。
- テスト版は **v2026.06.24-test.111**。テスト版の伝票撮影だけを専用 `api/extract_address_test.php` へ切り替え、通常版の `api/extract_address.php` は変更していない。
- OCRへ建物名と部屋番号を別項目で返すよう指示した。住所末尾の `102`、`203`、`1001`、`C-413` などが部屋番号なら住所へ連結せず、備考へ `号室` として保存する。
- AIが部屋番号を住所へ含めた場合も、明示された部屋番号、`号室` 表記、階数と部屋位置が自然な3〜4桁の末尾番号の順でテスト版端末側が再分離する。通常の丁目・番・号や部屋番号らしくない末尾番号は維持する。
- 〒103-0015と日本橋箱崎町、〒103-0014と日本橋蛎殻町を照合し、OCRがこの2町を取り違えた場合は郵便番号を優先して町名を補正する。旧字体の `蠣殻町` も照合対象に含める。
- Geminiのモデル、画像サイズ、呼び出し回数は従来どおりで、通常時は1写真につき1回のOCRを維持する。追加のAI再読取や住所API待ちは加えていない。

---

## 0. Codex追記（2026-08-07 / test.110）

- 作業開始基準は最新 `origin/main` のコミット `1c86e49cd191a7ab082b7ff722eaa9e8f5f474bd`。
- 安定版は引き続き **v2026.07.10-1**。通常版HTML・通常版API・共有サーバーには今回の変更を反映していない。通常版DB APIの `manual=1` 保護と通常版HTML内の古い固定バージョン表示も、昇格指示がないため未変更。
- テスト版は **v2026.06.24-test.110**。既存のルート項目は残し、メニューへ `時間指定込み・安全優先（試験）` を追加した。この項目だけが時間指定荷物を配送順へ組み込む。
- `時間指定設定` では時間帯ごとに、`開始〜終了の時間内に配達`、`終了時刻までに配達`、`ルート計算には使わない` を明示して開始・終了時刻を保存する。既存の標準時間帯は従来の表示内容に対応する時間内指定へ移行し、新規の独自時間帯は誤解釈を防ぐため初期状態を `ルート計算には使わない` とする。
- `指定時刻（任意）` は、指定時刻の前後（初期15分）または指定時刻まで、のどちらとして扱うかを時間指定設定で選べる。
- 新ルートは最初に従来のエリア相談を行い、確認したエリア順とMapboxの道路優先順を土台にする。締切の安全余裕（初期20分）へ間に合わない場合だけ時間指定荷物を前へ動かし、時間帯の開始前に着く場合は待機時間も終了見込みへ加える。確定前に時間指定ごとの到着見込み、待機、余裕不足、遅延見込みを表示する。
- 1件あたりの作業時間は3・5・7分から選べる。自動調整を有効にすると、当日の連続する完了実績から概算移動時間を除いた直近最大12区間の中央値を使い、有効な3区間がたまった後に3・5・7分へ段階補正する。
- 配送途中で実績が当初より遅くなった場合、現在の配送順を勝手に変更せず `再計算` を表示する。利用者が新ルートを開き、到着見込みを確認して確定した時だけ新しい順番を適用する。
- 時刻と道路時間は予測であり、渋滞・駐車・建物内作業・未登録の道路規制を完全には保証しない。時間に間に合わない見込みも隠さず確認画面へ表示する。

---

## 0. Codex追記（2026-08-06 / test.109）

- 作業開始基準は最新 `origin/main` のコミット `d512a36b377855914e54faf9902a997063dbea8b`。
- 安定版は引き続き **v2026.07.10-1**。通常版HTML・通常版API・共有サーバーには今回の変更を反映していない。
- テスト版は **v2026.06.24-test.109**。
- 保存済み伝票写真、処理待ち写真、サイト全体の端末使用容量を `写真・個人情報管理` で確認できる。完了から7日を過ぎた伝票写真だけを起動時・アプリ復帰時に自動整理し、未完了と処理待ち写真は対象外にする。
- 完了済み写真だけの一括削除と、未完了を含む保存済み写真の全削除を確認付きで追加した。配送先と配送実績は写真と分離し、写真だけを削除できる。
- 任意の4〜8桁簡易PINを端末ごとに設定できる。PINは平文保存せず、ランダムsalt付きSHA-256で照合し、起動時と5分以上バックグラウンドに置いた後にロックする。写真自体の暗号化ではないため端末画面ロックとの併用を案内する。
- 配送モード開始、確定した配送順、完了順、前の完了からの所要時間、案内線の手動引き直し、ピン位置修正、手動通行規制登録を端末内へ記録する。日別画面で予定順と実績順、平均所要時間、各回数を確認し、CSVコピー・保存を行える。
- 配送実績はこの端末だけに90日保持し、共有コードのAPIやほかのスマホへ送信しない。日別削除と全削除を確認付きで用意した。

---

## 0. Codex追記（2026-08-06 / test.108）

- 作業開始基準は最新 `origin/main` のコミット `c91cbc792c701b2bbb18f89b9a4838320346cca6`。
- 安定版は引き続き **v2026.07.10-1**。通常版HTML・通常版API・Google案内には今回の変更を反映していない。
- テスト版は **v2026.06.24-test.108**。
- 1枚撮影、連写、写真一括読み込みでは、OCR・住所検索を始める前に元写真と入力途中の荷物情報をIndexedDBへ退避する。アプリ終了、通信切断、処理途中の再起動後に待機写真を読み込み、通信回復時またはアプリ復帰時に処理を再開する。
- 処理待ち写真ごとの固定IDを伝票写真と配送先へ記録し、保存直後にアプリが終了して同じ写真を再処理しても、新規ピンの重複作成や複数個口の二重加算をしない。
- 配送リストは通常保存に加えて直前の正常な内容を1世代予備保存し、主データのJSONが壊れた場合は予備データから自動復元する。全件削除・完了分削除では、削除済み住所を予備データへ残さない。
- メニューへ `保存・復旧状況` を追加し、最終保存時刻、配送先復元数、写真の処理中・再開待ち・再処理必要件数、案内線キャッシュ、GPS、通信状態を確認できる。手動再処理と失敗写真の確認付き削除も行える。
- GPS取得がタイムアウトまたは一時停止した時は短時間後に再試行し、アプリが前面へ戻った時にもGPSと写真処理を再開する。処理待ち写真は個人情報を長期間残さないため3日を超えると自動整理する。

---

## 0. Codex追記（2026-08-06 / test.107）

- 作業開始基準は最新 `origin/main` のコミット `f011839bd9bc26e9978d6c9e8324cf27374129e9`。
- 安定版は引き続き **v2026.07.10-1**。通常版HTML・通常版API・Google案内には今回の変更を反映していない。
- テスト版は **v2026.06.24-test.107**。
- 3点の通行規制登録を `右左折・Uターン禁止（3点）` と明示した。Uターン禁止は、進入側→Uターン地点→反対向きに戻る道路の順で指定する。
- 3点の角度から右折禁止・左折禁止・直進禁止・Uターン禁止を判定し、保存確認画面、地図ラベル、登録一覧、保存完了通知に判定結果を表示する。
- 経路APIの3点通行規制判定は `test.106` からUターン形状にも対応済みであり、今回の変更はその登録方法と判定結果を利用者に明示するもの。共有・安全経路選択・回避不能時に案内線を出さない動作は維持する。

---

## 0. Codex追記（2026-08-06 / test.106）

- 作業開始基準は最新 `origin/main` のコミット `a3a832783fcf4b9bbbbbdf5f943efeb7acd84b18`。
- 安定版は引き続き **v2026.07.10-1**。通常版HTML・通常版API・Google案内には今回の変更を反映していない。
- テスト版は **v2026.06.24-test.106**。
- `Mapbox案内線: 通行規制・進入方向` に、道路全体を避ける1点登録、一方通行・進入禁止の2点登録、右左折禁止の3点登録を追加した。一方通行は通行不可の向き、右左折禁止は進入側→交差点→禁止側の順に指定する。
- Mapbox Directionsから複数の候補経路を取得し、手動登録した禁止方向または禁止右左折を通る候補を除外する。安全な方向の候補がない時は対象道路全体を回避点に変換して再探索し、それでも回避できない時は危険な案内線や保存済みの古い線を表示しない。
- 手動通行規制と配送先ごとの到着方向は、既存の `デバイス間データ共有` と同じ共有コードを設定した端末間だけで10秒ごとに共有する。配送リストとは内部の共有領域を分離し、削除履歴も同期して他端末から古い規制が復活しないようにした。
- 変更対象は `test/index.html`、`test/manual.html`、テスト専用の `api/route_segment_test.php`、`api/route_flow_test.php`、共通判定ライブラリだけ。配送順の最適化計算とGoogle案内は変更していない。

---

## 0. Codex追記（2026-08-06）

- 作業開始基準は最新 `origin/main` のコミット `8a5cf8b4f5022d2c2c5f76fad59df9009164275b`。
- 安定版は引き続き **v2026.07.10-1**。通常版HTML・通常版API・Google案内には今回の変更を反映していない。
- テスト版は **v2026.06.24-test.105**。
- メニューの `Mapbox案内線: 通れない道路・進入方向` から、現場で通れない道路の中央付近を地図上へ登録できる。登録地点は赤い `×` で表示し、次回取得するMapboxのオレンジ・青線へ最大50地点の回避指定として渡す。
- 配送先編集画面の `今の到着方向を記憶` で、GPSの進行方向または直近の移動方向を住所単位に保存し、次回のMapbox案内で同じ方向からの進入を優先する。1番の近くを通って少し歩く停車候補も、記憶した進入方向と逆なら採用しない。個別に解除できる。
- Mapboxが持つ一方通行・右左折規制の道路情報は従来どおり使い、今回の手動記憶でデータにない規制・工事・実際に通りにくい道を補う。回避不能時は現地標識を優先する警告を出す。
- 通れない道路と進入方向はテスト版の端末内だけに保存し、デバイス間共有データへは加えていない。アプリデータやサイトデータを消すと消える。
- 手動で直したピン位置は従来どおり同一住所の自動検索より優先される。配送先編集画面に `手動で直したピン位置を記憶済み` と表示し、記憶状態を確認できるようにした。
- 通行不可地点はMapboxのオレンジ・青線に適用する。配送順の最適化計算そのものとGoogle案内には適用しない。

---

## 0. Codex追記（2026-08-03）

- 作業開始基準は最新 `origin/main` のコミット `8c5eefd7b7cbe4c9e631857f075ee855192022be`。
- 安定版は引き続き **v2026.07.10-1**。今回の変更は反映していない。
- テスト版は **v2026.06.24-test.104**。
- 集荷モードではリスト見出しの `入力 / 配送` 切替を表示せず、伝票の1枚撮影、連写、手入力、メニュー内の連続撮影・写真一括読み込みを利用できないようにした。
- 集荷モードは入力用バーのないレイアウトへ自動で切り替え、集荷へ移る時に連写が有効なら終了する。非表示の入力切替を外部から呼ばれても集荷中は入力モードへ変わらない。
- 配送モードで最後に選んだ `入力 / 配送` は保持し、集荷から配送へ戻った時に復元する。
- テスト版Webだけの変更で、インストール型AndroidアプリもAPKを入れ替えずに反映される。

---

## 0. Codex追記（2026-08-01）

- 作業開始基準は最新 `origin/main` のコミット `7727671b67d587d780be131770c7648f9c3aaa2f`。
- 安定版は引き続き **v2026.07.10-1**。今回の変更は反映していない。
- テスト版は **v2026.06.24-test.103**。
- ピンから開く `配送先を編集` では、同じ配送先に複数の伝票写真がある場合、写真を左右へフリックして前後の写真へ切り替えられる。縦スクロールは従来どおり利用できる。
- 写真の拡大表示でも、等倍時は左右フリックで前後へ切り替え、上フリックで背後の `配送先を編集` へ戻れる。
- 写真を2本指またはダブルタップで拡大している間は、左右・上フリックより既存の一指移動を優先する。ピンチ拡大・縮小、ダブルタップ、左右ボタン、Androidの戻る操作は維持した。
- テスト版Webだけの変更で、インストール型AndroidアプリもAPKを入れ替えずに反映される。

---

## 0. Codex追記（2026-07-31）

- 作業開始基準は最新 `origin/main` のコミット `3cac91ae612087ad9c802b66b8b5ca4d03599404`。
- 安定版は引き続き **v2026.07.10-1**。今回の変更は反映していない。
- テスト版は **v2026.06.24-test.102**。
- `test.101` でも一時案内帯に配送アプリモード限定の条件が残り、集荷モードから一時案内を開始すると帯が隠れていた。一時案内先がある場合は入力画面・配送画面・集荷モードのすべてで帯を表示し、通常の次目的地帯だけを配送画面に限定した。
- `test.100` で通常案内帯を配送モード限定にした際、一時案内帯まで入力モードで隠れる回帰が入った。通常案内は配送モード限定のまま、一時案内は入力モード・配送モードの両方で表示するよう修正した。
- 地図下部の案内帯を一時案内だけでなく通常の配送順でも表示し、次の目的地の住所、表示中のオレンジ道路線と徒歩区間を合計した概算距離、現在地からの引き直しボタンを出す。
- 案内帯の `↻` は通常配送と一時案内の両方で現在地から道路線を再取得する。帯の `↩ 戻す` は削除し、一時案内の解除は対象ピンの編集画面にある `元のコースへ戻す` または配送完了で行う。
- インストール型Androidアプリのネイティブカメラは縦固定を維持し、EXIFの向きだけをJPEG本体へ反映する。OCRの回転再試行は読み取り用コピーだけに限定し、その追加回転を保存写真へ反映しない。写真確認画面の手動回転ボタンも削除した。
- 配送順、全体道路形状、現在地→1番のオレンジ線、1番→2番の青線、徒歩区間・停車候補、一時案内先を端末内へ当日18時間キャッシュする。アプリ再起動時は対象ピンのIDと座標を照合して復元し、最初のGPS取得後に現在地から引き直す。
- 完了・削除・住所再検索・ピン位置修正・地図プロバイダー変更では一致しない道路形状を破棄し、古い線を別の配送先へ表示しない。道路APIの一時失敗時は、同じ対象に限って直前の線を再表示する。
- テスト版Webだけの変更で、インストール型AndroidアプリもAPKを入れ替えずに反映される。すでに保存済みの写真は再変換せず、新しく撮影・保存する写真から向き補正を適用する。

---

## 0. Codex追記（2026-07-30）

- 作業開始基準は最新 `origin/main` のコミット `cc22fb802dd3f0c7cfcad28e84eba302cdcb3a18`。
- 安定版は引き続き **v2026.07.10-1**。新機能は反映していない。
- テスト版は **v2026.06.24-test.97**。
- 配送先編集の伝票写真を開いた後、2本指で最大4倍まで拡大・縮小、拡大中は1本指で移動、ダブルタップで拡大と等倍を切り替えられる。
- 郵便番号だけが検索結果と違う場合は要確認にせず、郵便番号に加えて町名または番地も不一致の場合だけ郵便番号不一致を表示する。番地照合から検索結果側の郵便番号数字を除外した。
- 連写の要確認を修正保存した時は、連写カメラ上部の要修正件数を1件減らし、修正済みの荷物を追加件数へ移す。0件では要修正表示を出さない。
- 配送リストの住所表示から郵便番号だけを除外した。保存住所、検索、編集画面、住所判定では郵便番号を保持する。
- テスト版Webだけの変更で、インストール型AndroidアプリもAPKを入れ替えずに反映される。

---

## 0. Codex追記（2026-07-28）

- 作業開始基準は最新 `origin/main` のコミット `541ca472a4a100d75f6713a46f0ebec467c9bf65`。
- 安定版は引き続き **v2026.07.10-1**。新機能は反映していない。
- テスト版は **v2026.06.24-test.96**。インストール型Androidアプリを縦向き固定にし、伝票撮影から撮影直後の配送先編集・荷物情報入力まで同じ縦向きを維持する。
- Androidは `MainActivity` の `android:screenOrientation="portrait"` でアプリ全体を固定する。APKは `versionCode 2`、`versionName 2026.06.24-test.96`。
- テストPWAの `manifest.webmanifest` にも `"orientation": "portrait"` を追加した。通常のブラウザータブでは端末・ブラウザーの制約により強制できない場合がある。
- Androidネイティブ設定の変更なので、インストール型アプリへの反映には最新版APKへの入れ替えが必要。
- APK配布は引き続きCI生成のデバッグ署名であり、既存APKと署名が異なる場合は上書きできない。その場合は端末内データが不要であることを確認して旧アプリをアンインストール後、最新版をインストールする。

---

## 0. Codex追記（2026-07-27）

- 作業開始基準は最新 `origin/main` のコミット `aa00825cf73098c5c183cde75826786c84958281`。
- 安定版は引き続き **v2026.07.10-1**。新機能は反映していない。
- テスト版は **v2026.06.24-test.95**。目的地接近時のGoogleからMapboxへの自動切替を廃止し、地図は利用者が `Mapbox / Google` ボタンで切り替える。
- 手動切替時は中心座標と縮尺をGoogle→Mapbox、Mapbox→Googleの双方向で引き継ぐ。両地図のタイル基準差を補正するため、同じ見た目の縮尺はGoogleをMapboxより1段大きくする。
- GPS追跡中は位置更新で縮尺を17へ戻さず、利用者が変更した縮尺を保ったまま現在地へ中心だけ追従する。
- Google地図でも、素早く2回タップして2回目の指を離さず上下へ動かす一指ズームを利用できる。上へ動かすと縮小、下へ動かすと拡大する。
- 通常の一指移動と二指ピンチは従来どおりGoogle Mapsへ渡し、ピンタップ・長押しとの競合を避けるため一指ズーム中だけGoogle側のタッチ処理を止める。
- テスト版Webだけの変更なので、インストール型AndroidアプリもAPKを入れ替えず反映される。

---

## 0. Codex追記（2026-07-26）

- 作業開始基準は最新 `origin/main` のコミット `73afeadc4f5966e9e1a937fb581a58fa0e18430d`。
- 安定版は引き続き **v2026.07.10-1**。新機能は反映していない。
- テスト版は **v2026.06.24-test.93**。最新版のインストール型Androidアプリで戻る三角をアプリ内で受け取り、写真拡大中は編集画面へ、`配送先を編集` の表示中はマップへ戻る。
- アプリ内カメラ上部へ `🔦 OFF / ON` を追加。端末が対応していれば `torch` で連続点灯し、1枚撮影・連写の両方で利用できる。カメラ終了時は必ず消灯する。
- ライトは既存の `@capacitor-community/camera-preview` **8.0.1** を使用するため、テスト版Webの更新だけで反映され、追加のAPK入れ替えは不要。
- Androidシェルへ公式 `@capacitor/app` **8.1.1** を追加。この改善にはAPKの更新が必要。既存APK向けのWeb履歴による互換案は、旧APK実機相当テストでAndroidがWebより先にアプリを終了すると判明したため不採用。
- 現在のAPK配布はCIごとに異なるデバッグ署名で生成されており、既存アプリへ上書き更新できない。端末内の写真・配送先が必要な間は旧アプリをアンインストールしないこと。今後の継続更新には固定署名鍵の設定が必要。

---

## 0. Codex追記（2026-07-25）

- 作業開始基準は最新 `origin/main` のコミット `3776e64e33384c6f0f2d34664f41597208d55293`。Google案内の本体はコミット `74ba23b882fc4e859f5acc46fae41bf30c93cdd5` で `main` へ反映済み。
- 安定版は引き続き **v2026.07.10-1**。新機能は反映していない。
- テスト版は **v2026.06.24-test.89**。Google経路の残りが約150mになると番地確認用のMapboxへ自動切替し、完了後は次のGoogle案内へ戻る。GoogleからMapboxへの切替時は中心位置と表示範囲を同期し、自動切替、手動切替、ピン長押しの位置修正で表示地点や縮尺が飛ばない。手動でGoogleへ戻した配送先では再切替しない。Google地図のピン番号は配送リストと同じ固定番号。
- 配送モードの地図上に `Mapbox / Google` の切替を追加。Google Routes APIの案内線は利用規約に従いGoogle地図上だけに描画し、Mapboxは番地・建物・ピン位置修正に引き続き使う。
- Google案内では現在地→次の配送先をオレンジ、次の配送先→その次を青で表示し、残りの配送先、時間指定、集荷ピンもGoogle地図へ表示する。Google地図または経路取得に失敗した場合はMapboxへ自動復帰する。
- テスト専用APIは `api/google_guidance_test.php`、`api/google_maps_config_test.php`、`api/route_segment_test.php`。Mapbox側も現在時刻、走行方向、速度、GPS精度を使って出発直後の不要な折り返しを抑える。
- すぐ旧表示へ戻す場合は画面上の `Mapbox` を選ぶ。機能全体を止める場合は `test/index.html` の `GOOGLE_GUIDANCE_FEATURE_ENABLED` を `false` にする。Google案内本体は単独コミット `74ba23b` のため、コミット単位でも撤回できる。
- Google地図表示には、HTTPリファラー制限とMaps JavaScript API制限を設定したブラウザーキーを使用する。サーバー上ではGit管理外の `api/google_maps_browser_key_test.runtime.php` に保存し、サーバー用キーはブラウザーへ公開しない。
- ブラウザーキーの一度限りの設定処理は完了後にサーバーとリポジトリの両方から削除済み。以後のデプロイではランタイム設定ファイルを除外して保持する。
- **通常版DB API `api/delivery_geocode_cache.php` には、テスト版にある `manual=1` の保護処理がまだ未反映。通常版昇格時に必ず別途確認すること。**
- **通常版 `index.html` のメニュー内固定バージョン表示には古い値が残るが、通常版昇格の指示がないため今回は変更しない。**

---

**宛先: Codex**
このドキュメントは、Claude（別AIセッション）が `delivery_map_mapbox/` と `capacitor/` に行った作業の最新報告です。前回の報告（2026-06-12, v2026.06.12-test.1）から大きく進んでいるため、**過去の報告は破棄してこのファイルを唯一の最新状態として扱ってください。**

**同じ内容を二重に実装しないでください。** また、下記の「開発ルール」を必ず守ってください。

---

## 1. バージョンの現状（2026-07-10 時点）

| 対象 | バージョン | 状態 |
|---|---|---|
| 安定版 `index.html` | **v2026.07.10-1** | 本日、テスト版 test.54 を全面昇格。デプロイ済み（Actions run #259 success） |
| テスト版 `test/index.html` | **v2026.06.24-test.54** | 安定版と機能同等。今後の新機能開発はここで行う |
| Androidアプリ (APK) | test.54 相当 | `capacitor/` のCapacitorシェル。**テスト版URLを読み込む。安定版用APKは作らない（ユーザー指示）** |

- 安定版と テスト版は現在**機能的に同一**（差分は名前空間・URL・ブランディングのみ）。
- 前回の昇格は 2026-07-07（test.46 → v2026.07.07-1, コミット `e581432`）。今回は test.47〜54 の差分を追加昇格（コミット `cc644f2`）。

---

## 2. 前回報告(6/12)以降に実装された主要システム

すべて**テスト版で実装 → 検証 → 安定版へ昇格済み**。両方に入っています。

### 地図表示
- **全国番地表示エンジン**: Geolonia japanese-addresses-v2 API（ja.jsonカタログ → 市区町村ごとの machiaza CSV を HTTP Range で取得）。ズームに応じて番地ラベルを描画。IndexedDB（`rys-mapbox(-test)-banchi` DB）に30日キャッシュ、GPS位置から先読み。診断パネルあり（メニュー→番地診断）。
- **筆界（地番境界）ライン**: 法務省登記所備付地図データ（amx-project）の PMTiles を `api/amx_tile_proxy.php` 経由で読む。レイヤは daihyo(z2-13)/fude(z14-16)。3D建物に隠れないよう slot:'top'。
- **起動高速化**: Service Worker（stale-while-revalidate でアプリ本体を即表示）、設定キャッシュ、前回表示位置の復元、preconnect。地図が上半分しか描画されない起動バグも修正済み。

### 伝票OCR（Gemini 2.5-flash-lite, `api/extract_address.php`）
- プレビュー表示中にOCRを先行実行、バイナリPOST送信（base64レガシーも受付）
- **回転自動リカバリ**: APIが `rotation_hint` を返し、失敗時は hint→180→90→270 の順で最大4回リトライ
- **発送元の除外**: 住所が2つ写った場合、GPS現在地に近い方（=お届け先）を採用。発送元にはピンを立てない
- **郵便番号アンカー**: 「東日本橋」を「日本橋」に誤読するような欠落を郵便番号で補正
- **要確認ピン検証** (`validateDeliveryPlacement`): ジオコード結果が住所と食い違う場合は「要確認」ピンにして警告
- 自信度（confidence）低下時の警告表示

### 荷物運用
- **複数個口**: 同じ住所を再スキャンすると重複ブロックせず、1本のピンに個数を積み上げ表示
- **連続撮影モード**: マップに戻らず連続スキャン。手入力も連続入力モードあり
- **まとめて読み込み**: 標準カメラアプリで撮った写真をギャラリーから一括インポート（並列4で処理）
- **手入力の番地・号は常に空欄で開始**（前回値の復元は禁止 — ユーザー明示要求。3箇所で漏れを潰した経緯あり）
- **手動ピン修正の記憶**: ピンを手で直すと共有ジオコードキャッシュ（MySQL, `api/delivery_geocode_cache(.php/_test.php)`）に `manual=1` で保存。**自動保存は `manual=1` の行を絶対に上書きしない**（`IF(manual=1,...)` で保護）
- **時間指定の自動読み取りは削除済み**（誤作動多発のため）。手動設定機能は残存

### マニュアル
- `manual.html`（安定版・テスト版それぞれ）を全面書き直し済み（コミット `30fe7d5`）。**機能を変えたらマニュアルも更新するのが恒久ルール。**

---

## 3. Androidアプリ（Capacitorシェル）— 新規追加

`capacitor/` ディレクトリと `.github/workflows/android-apk.yml` を新設。

- **目的**: 連写（ネイティブカメラでの高速連続撮影＋バックグラウンドでのピン処理）はブラウザでは実現できないため、Android のみアプリ化
- **構成**: Capacitor 8。`server.url = https://rys-services.com/delivery_map_mapbox/test/` — アプリの中身はライブのテスト版を読むだけなので、**Web側の変更はAPK再ビルド不要で即反映**
- **ネイティブカメラ**: `@capacitor-community/camera-preview`（toBack:true, WebView透過必須 → `MainActivity.java` で `setBackgroundColor(TRANSPARENT)` 済み）。連写モードと通常1枚撮影の両方がネイティブカメラを使用。Android 11+ の `<queries>`(IMAGE_CAPTURE) もマニフェストに追加済み
- **CI**: main への `capacitor/**` push で APK をビルドし、FTPで `app/rys-map.apk` として配布（https://rys-services.com/delivery_map_mapbox/app/rys-map.apk）。Node 22 / Java 21 必須
- **安定版・index.html 側にもネイティブカメラのコードは入っているが**、`window.Capacitor` の存在チェックでガードされており、**ブラウザ/PWAでは完全に無効（従来どおり file input → OSカメラ）**
- **安定版URL用のAPKは作らない**（ユーザー指示: まだ不要）。iPhone版も当面作らない（配布コストの問題）

### ⚠️ 過去の失敗（繰り返さないこと）
- **ブラウザの getUserMedia でのアプリ内カメラは実装済み→撤回済み**（`d813537` → revert `47176a2`）。ピント・画質が実用に耐えずOCR精度が落ちた。「OCR精度を落とさない」が絶対条件。Webでのカメラ内製化は提案しないこと。

---

## 4. 開発ルール（必読・変更禁止）

1. **新機能はすべて `test/` に実装。** 安定版 `index.html` はユーザーが明示的に「昇格して」と言った時だけ更新する
2. **昇格は機械的変換**で行う（今回 `cc644f2` の手順）:
   - `../api/` → `api/`（テスト版は1階層深い）
   - `delivery_geocode_cache_test.php` → `delivery_geocode_cache.php`
   - `rys-mapbox-test-*` → `rys-mapbox-*`（localStorage/IndexedDBキー 36個）
   - `テスト版` ブランディング除去（title / h1バッジ / 通知文言）
   - Service Worker: キャッシュ名 `rys-test-*`→`rys-*`、パス `/test/` 除去、通知タイトル
   - `APP_VERSION` とキャッシュバスター（`?v=`）を新しい日付版に
   - 変換後に残存マーカーがゼロであることを検証してからコミット
3. **`APP_VERSION` は変更のたびに必ず上げる**（テスト版は `v2026.06.24-test.NN` の NN をインクリメント）
4. **コミット前に構文チェック**: インライン `<script>` を抽出して `node --check`
5. **デプロイ**: main への `delivery_map_mapbox/**` push で GitHub Actions が FTP デプロイ（約1分）。`capacitor/**` push で APK ビルド
6. マニュアル（`manual.html` 両版）を機能変更に追随させる
7. 共有ジオコードキャッシュの `manual=1` 行を自動処理で上書きしない

---

## 5. 主要ファイル

| ファイル | 役割 |
|---|---|
| `delivery_map_mapbox/index.html` | 安定版本体（v2026.07.10-1, 約8900行） |
| `delivery_map_mapbox/test/index.html` | テスト版本体（test.54）— 開発はここ |
| `delivery_map_mapbox/service-worker.js` / `test/service-worker.js` | SWキャッシュ（shell: SWR / static: cache-first / APIは非キャッシュ） |
| `delivery_map_mapbox/manual.html` / `test/manual.html` | 利用マニュアル（両版） |
| `api/extract_address.php` | Gemini OCR（バイナリPOST + base64、rotation_hint返却） |
| `api/delivery_geocode_cache.php` / `_test.php` | 共有ジオコードキャッシュ（MySQL, manualフラグ保護） |
| `api/amx_tile_proxy.php` | 法務省 PMTiles プロキシ（筆界ライン用） |
| `capacitor/` | Capacitor Androidシェル（appId `com.rys.deliverymap`） |
| `.github/workflows/deploy-delivery-map.yml` | Web FTPデプロイ |
| `.github/workflows/android-apk.yml` | APKビルド＆FTP配布 |

## 6. 前回昇格(7/7)以降のコミット一覧

| コミット | 内容 |
|---|---|
| `6d6b772` | 手動ピン修正の住所別記憶（manual保護付き） |
| `09271e9` | 時間指定の自動検出を削除（手動のみに） |
| `c0c4f13` / `bb1a67e` | 写真まとめて読み込み（並列4） |
| `30fe7d5` | マニュアル全面書き直し（両版） |
| `1dcad5a` / `02804ae` | Capacitor Androidシェル + APK CI（Node 22） |
| `e3bb374` / `76504b9` | ネイティブ連写カメラ + 白画面修正（WebView透過） |
| `ff506c2` | APKでの通常1枚撮影修正（`<queries>` + ネイティブ単写モード） |
| `cc644f2` | **test.54 → 安定版 v2026.07.10-1 へ昇格** |

---

*このファイルは Claude が 2026-07-10 に更新。質問があればユーザー経由で確認してください。*
